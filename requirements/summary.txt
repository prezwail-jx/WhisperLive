# Local LLM summary service dependencies.
# Installed after requirements/server.txt in docker/Dockerfile.server.
# Keep NumPy 1.x because onnxruntime==1.17.0 is not compatible with NumPy 2.x.
numpy<2
vllm
