# WhisperLive 实时同传使用指导手册

本文档面向会议使用者，说明如何选择业务模式、开始会议、查看字幕、使用热词，以及在会后导出日志和生成会议总结。普通用户不需要了解服务器或 GPU 的分工。

## 1. 使用入口

请使用最新版 Chrome、Edge 或 Safari 打开：

```text
https://app.cmtbs.com:57890/
```

首次使用时，浏览器会请求麦克风权限，请选择“允许”。中控页面用于运维管理，普通参会用户不需要打开。

## 2. 选择业务模式

页面顶部提供四种模式：

| 模式 | 适用场景 | 默认行为 |
| --- | --- | --- |
| 普通同传 | 日常会议、实时字幕和中英翻译 | 优先兼顾实时性与翻译质量 |
| 高精同传 | 正式会议、翻译质量优先 | 使用更高质量的翻译模型，资源占用更高 |
| 对话翻译 | 中英文双方轮流交流 | 自动识别中文或英文并互译 |
| 语音识别 | 只需要会议原文 | 关闭翻译，只显示识别结果 |

选择模式后，系统会自动调整连接地址和翻译配置。设置中的“WebSocket 地址”通常保持默认，不需要手动修改。

### 2.1 普通同传

普通同传适合大多数会议，可以自动互译或手动指定中文 → English、English → 中文，页面会自动选择标准翻译模型。

### 2.2 高精同传

高精同传适合对译文质量要求较高的会议。页面会自动选择高精连接和当前可用的高质量翻译模型。

高精模式会占用更多服务器资源。如果出现连接繁忙、翻译延迟或翻译失败，请联系运维确认服务负载，必要时切换为普通同传。

### 2.3 对话翻译

对话翻译会自动处理中英文双方轮流发言，不需要手动切换源语言，适合面对面沟通、访谈和临时交流。

### 2.4 语音识别

语音识别模式只记录原文，不生成译文。请选择实际发言语言；页面会自动切换为单栏原文显示，会议日志仍可在会后导出、校对和用于生成总结。

## 3. 开始会议

1. 打开页面，在顶部选择业务模式。
2. 根据需要选择自动互译或指定翻译方向。
3. 点击右上角“设置”。
4. 填写会议名称，建议使用容易识别的名称，如“2026 产品周会”。
5. 如需说话人区分，开启“说话人识别”。
6. 如有专有名词，开始前上传热词文件。
7. 保持 WebSocket 地址为系统自动设置的值。
8. 点击“开始会议”，并允许浏览器使用麦克风。

会议开始后，业务模式、语言、连接地址、翻译模型、说话人识别和热词会被锁定。如需更换，请先停止当前会议。

## 4. 语言与翻译方向

普通同传和高精同传支持两种翻译方式：

| 翻译方式 | 说明 |
| --- | --- |
| 自动识别语言 | 自动判断中文或英文，并翻译成另一种语言 |
| 指定翻译 | 明确选择源语言和目标语言，适合单一语言会议 |

对话翻译始终使用自动互译；语音识别不启用翻译。翻译模型会根据业务模式和服务器可用模型自动选择，普通用户通常不需要修改“翻译模型”。

## 5. 会议热词

热词用于提高人名、公司名、产品名和专业术语的识别稳定性，也可以指定固定翻译。

### 5.1 文件格式

支持 UTF-8 编码的 `.txt` 和 `.md` 文件，建议一行一个词：

```text
WhisperLive
产品名称
大模型
同传系统
```

空行、以 `#` 开头的注释和常见 Markdown 列表标记会被忽略。不要把大段说明文字作为热词上传。

### 5.2 固定翻译

使用 `原词=>目标译文` 指定固定翻译：

```text
WhisperLive=>WhisperLive
大模型=>large language model
同传系统=>simultaneous interpretation system
```

固定翻译是单向规则。如需双向固定翻译，应分别增加两个方向的条目。

### 5.3 上传与生效

1. 打开“设置”。
2. 在“会议热词”中选择文件。
3. 确认页面显示文件名、热词数量和固定翻译数量。
4. 再点击“开始会议”。

热词仅对本次开始时锁定的会议有效。会议进行中更换文件不会改变当前连接，请停止后重新开始。

## 6. 显示与全屏

页面提供四种内容布局：

| 布局 | 说明 |
| --- | --- |
| 双栏 | 原文和译文左右排列 |
| 上下 | 原文和译文上下排列，默认布局 |
| 交错 | 每条原文下方显示对应译文 |
| 单栏 | 只显示原文、中文、English 或译文 |

在“设置 → 显示样式”中可以分别调整原文和译文的字号、颜色，也可以恢复默认显示。

点击“全屏”后，页面只保留字幕区域，并按设置字号放大显示。退出全屏可按 `Esc`，或使用浏览器的退出全屏操作。“清空”只清除当前页面显示，不会删除后端已经保存的会议日志。

## 7. 停止、断线与继续会议

会议正常结束时点击“停止会议”。停止后，后端会将本次会议标记为已结束，随后才能校对、导出和生成会议总结。

如果网络或 WebSocket 意外中断：

- 页面会自动尝试重连。
- 重连期间的音频不会被记录。
- 自动重连失败后，可以选择继续会议或结束中断的会议。
- 继续会议会沿用原会议记录，时间轴接续保存。

不要为了更换模式或连接地址直接刷新页面，应先停止会议。

## 8. 日志导出与校对

点击右上角“总结”，在“会议总结”面板中选择已经结束的会议，可以：

- 下载 Markdown 会议原文。
- 下载 DOCX 会议原文。
- 下载 DOCX 中英对照。
- 加载校对内容，修改识别文本或恢复原文。
- 新增、重命名、合并说话人，并重新分配片段。

校对内容由后端保存。修改原文后，已有译文和总结可能被标记为过期，需要重新生成总结。如果其他页面同时修改了同一会议，系统可能提示修订冲突并刷新为最新内容，请基于最新版本重新编辑。

## 9. 会议总结

### 9.1 使用条件

生成总结前需要满足：

1. 会议已经停止并出现在会议选择列表中。
2. 已选择需要的总结模板。
3. 后端总结服务可用且资源充足。

总结服务会按需启动大模型，首次生成可能需要较长时间。生产环境使用的总结模型资源占用较高，高负载时可能影响 ASR 或翻译。

### 9.2 生成步骤

1. 停止会议并打开“总结”。
2. 选择刚结束的会议。
3. 如有需要，加载并完成原文及说话人校对。
4. 选择内置模板或已保存的自定义模板。
5. 点击“生成总结”。
6. 完成后下载 Markdown 或 DOCX。

重新生成不会直接覆盖历史版本，可以在版本列表中选择并下载旧版本。

### 9.3 自定义模板

支持上传 `.md` 或 `.docx` 模板：

1. 选择模板文件并点击“分析模板”。
2. 检查自动识别的字段名称、类型和说明。
3. 调整后保存为自定义模板。
4. 从“自定义模板”分组中选择并生成总结。

删除自定义模板不会删除已经生成的历史总结，但之后不能再用该模板生成新总结。

## 10. 常见问题

### 10.1 点击开始后没有反应

检查浏览器是否允许麦克风、页面是否使用生产 HTTPS 地址，以及网络是否能访问 `app.cmtbs.com:57890`。WebSocket 地址应保持系统自动生成的值。

### 10.2 切换业务模式后地址发生变化

这是正常行为。页面会为不同业务模式自动选择正确的服务入口，不要手动改回旧地址。

### 10.3 有原文但没有译文

检查是否误选“语音识别”模式。其他可能原因包括翻译模型正在加载、语音尚未完成断句、服务器繁忙或网络中断。等待几秒仍无结果时，可停止会议后重新开始，或联系运维。

### 10.4 高精同传延迟较高或连接失败

高精模式使用更多服务器资源，且可能与普通会议并发运行。可临时切换普通同传，并请运维检查客户端数量、GPU 使用率、显存和后端日志。

### 10.5 热词没有明显效果

确认文件为 TXT/Markdown、每行一个词、开始会议前已经上传，并检查实际发音与热词差异。热词可以提高稳定性，但不能保证每次完全命中。

### 10.6 会议未出现在总结列表

只有已结束的会议会进入列表。确认已经点击“停止会议”；如果会议曾意外中断，请先选择结束中断会议。

### 10.7 总结生成失败

确认会议已停止、总结模板有效，并稍后重试。若提示模型启动失败、超时或显存不足，请联系运维处理，不要连续重复点击。

### 10.8 页面显示异常

先按 `Ctrl + F5` 强制刷新。刷新前如果会议仍在进行，请先停止会议，避免当前音频丢失。

## 11. 使用建议

- 日常会议优先使用普通同传。
- 对翻译质量有明确要求时使用高精同传。
- 中英文双方轮流交流使用对话翻译。
- 只需要原文记录时使用语音识别。
- 开始会议前填写会议名称并上传热词。
- 业务模式和 WebSocket 地址在会议中不要修改。
- 会后先校对原文和说话人，再生成最终总结。
