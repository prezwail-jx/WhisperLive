# WhisperLive 实时同传使用指导手册

本文档面向普通使用者，说明如何进入系统、选择识别服务、上传热词、查看译文、导出日志和生成会议总结。

## 1. 使用入口

请使用浏览器打开：

```text
https://app.cmtbs.com:57890/
```

建议使用最新版 Chrome、Edge 或 Safari。首次使用时，浏览器会请求麦克风权限，请选择“允许”。

中控页面地址为：

```text
https://app.cmtbs.com:57890/admin.html
```

普通参会用户通常只需要打开前端页面，不需要使用中控页面。

## 2. 识别服务选择

当前生产环境按以下方式使用：

| 使用场景 | WebSocket 地址 | 后端服务 |
| --- | --- | --- |
| 自动分流，不指定模型 | `wss://app.cmtbs.com:57890/ws` | 系统自动分配 |
| 固定使用 Whisper | `wss://app.cmtbs.com:57890/ws-gpu0` | GPU0 / Whisper |
| 固定使用 FunASR | `wss://app.cmtbs.com:57890/ws-gpu1` | GPU1 / FunASR |

如果需要明确选择识别引擎，请不要使用自动分流地址。

推荐选择：

| 场景 | 推荐填写 |
| --- | --- |
| 中英文会议、中英同传、翻译质量优先 | `wss://app.cmtbs.com:57890/ws-gpu0` |
| 中文会议、中文流式识别优先 | `wss://app.cmtbs.com:57890/ws-gpu1` |
| 临时测试、不关心后端服务 | `wss://app.cmtbs.com:57890/ws` |

## 3. 开始会议

1. 打开页面后，点击右上角“设置”按钮。
2. 在“会议名称”中填写本次会议名称。
3. 在“WebSocket 地址”中填写要使用的服务地址。
4. 根据会议场景选择源语言：
   - Whisper：中文或英文会议都适合，按实际主要发言语言选择 `中文` 或 `English`。
   - FunASR：中文会议通常选择 `中文`。
   - 中英文互相交流：开启“面对面模式”。
5. 如需翻译，保持翻译功能默认开启。
6. 如需使用会议热词，先上传热词文件，确认页面显示热词数量。
7. 点击“开始会议”。
8. 浏览器弹出麦克风权限时，选择“允许”。

## 4. 热词功能

热词用于提高会议中专有名词、人名、产品名、公司名等内容的识别稳定性，也可以用于指定固定翻译。

### 4.1 支持的文件格式

热词文件支持：

```text
.txt
.md
.docx
```

建议使用 `.txt` 或 `.md`，一行写一个热词。

示例：

```text
WhisperLive
FunASR
CTO
大模型
同传系统
```

### 4.2 固定翻译写法

如果希望某些词固定翻译，可以使用：

```text
原词=>目标译文
```

示例：

```text
WhisperLive=>WhisperLive
FunASR=>FunASR
大模型=>large language model
同传系统=>simultaneous interpretation system
```

这类写法会同时作为识别热词和翻译词表使用。

### 4.3 上传热词

1. 点击右上角“设置”。
2. 找到“会议热词”区域。
3. 点击“热词文件”，选择本次会议要使用的热词文件。
4. 上传后页面会显示文件名、热词数量和固定翻译数量。
5. 确认无误后再点击“开始会议”。

注意：

- 热词是本次页面会话内使用的配置。
- 如果要换热词文件，建议先停止会议，再重新上传并开始新会议。
- 热词文件不要放大段说明文字，尽量一行一个词或一个固定翻译。

## 5. 常用模式说明

### 5.1 普通翻译模式

适合单一语言输入。

| 输入语言 | 推荐识别服务 | 说明 |
| --- | --- | --- |
| 英文发言 | Whisper / GPU0 | 识别英文并翻译为中文 |
| 中文发言 | Whisper / GPU0 或 FunASR / GPU1 | Whisper 适合中英文；FunASR 更偏中文流式识别 |

### 5.2 面对面模式

适合中英文互相交流。

开启“面对面模式”后，系统会自动处理中英文互译。此时源语言选择会被锁定，不需要手动选择源语言。

面对面模式适合：

- 中英文双方轮流发言。
- 会议中语言可能频繁切换。
- 不希望用户手动切换源语言。

## 6. 显示方式

页面顶部可以选择显示模式：

| 模式 | 说明 |
| --- | --- |
| 双栏 | 左侧显示原文，右侧显示翻译 |
| 交错 | 一句原文一句译文排列 |
| 单栏 | 只显示翻译内容 |

如果只想看译文，建议选择“单栏”。

## 7. 停止、日志导出与校对

会议结束后，请点击“停止会议”。

停止后可以在“总结”面板中进行：

- 导出 Markdown 原文
- 导出 DOCX 原文
- 导出 DOCX 中英对照
- 加载校对内容
- 修正说话人
- 修正识别文本
- 生成会议总结
- 下载会议总结

注意：请先停止会议，再导出日志或生成总结。

## 8. 总结功能

总结功能用于根据会议原文生成会议纪要。它依赖后端大模型服务，显存占用较高。

### 8.1 使用前提

使用总结功能前，请确认：

1. 会议已经点击“停止会议”。
2. 当前没有正在进行的 ASR 实时识别任务。
3. 负责总结的大模型服务已经启动。
4. GPU 显存有足够空闲空间。

### 8.2 重要显存说明

总结模型显存需求较大。请尽量在 GPU 不进行 ASR 实时识别的时候使用总结功能。

不建议在以下情况下生成总结：

- 正在进行实时会议识别。
- GPU0 或 GPU1 正在承载多人 ASR 连接。
- `nvidia-smi` 显示显存已经接近满载。
- 后端日志出现 CUDA out of memory。

如果在 ASR 运行中强行生成总结，可能导致：

- 总结生成失败。
- ASR 延迟升高。
- 翻译不出结果。
- GPU 显存不足。
- 后端服务异常退出。

### 8.3 生成总结步骤

1. 点击“停止会议”。
2. 打开“总结”面板。
3. 在“会议日志”中选择刚结束的会议。
4. 如需校对，点击“加载校对内容”，修正原文或说话人。
5. 选择纪要模板。
6. 确认 GPU 当前空闲后，点击“生成总结”。
7. 生成完成后，可下载 Markdown 或 DOCX。

### 8.4 重新生成总结

如果校对了会议原文或说话人，可以重新生成总结。

每次生成会保留新版本，可在“下载版本”中选择不同版本下载。

## 9. 推荐使用方式

### 9.1 中英文会议或中英同传

WebSocket 地址填写：

```text
wss://app.cmtbs.com:57890/ws-gpu0
```

推荐设置：

- 识别服务：Whisper / GPU0
- 源语言：按主要发言语言选择 `中文` 或 `English`
- 如中英文混合交流：开启面对面模式
- 如有专有名词：提前上传热词文件

### 9.2 中文会议

WebSocket 地址填写：

```text
wss://app.cmtbs.com:57890/ws-gpu1
```

推荐设置：

- 识别服务：FunASR / GPU1
- 源语言：中文
- 如果需要英文译文，保持翻译开启
- 如有公司名、人名、产品名：提前上传热词文件

### 9.3 不确定使用哪个服务

WebSocket 地址填写：

```text
wss://app.cmtbs.com:57890/ws
```

说明：系统会自动分配到当前连接数较少的后端。但如果需要指定 Whisper 或 FunASR，请不要使用这个地址。

## 10. 常见问题

### 10.1 点击开始后没有反应

请检查：

- 浏览器是否允许麦克风权限。
- WebSocket 地址是否填写正确。
- 当前网络是否能访问 `app.cmtbs.com:57890`。
- HTTPS 页面是否使用了 `wss://` 地址。

正确格式示例：

```text
wss://app.cmtbs.com:57890/ws-gpu0
```

不要填写成：

```text
ws://app.cmtbs.com:57890/ws-gpu0
```

### 10.2 想用 Whisper，但好像识别效果不像 Whisper

请确认 WebSocket 地址是否填写为：

```text
wss://app.cmtbs.com:57890/ws-gpu0
```

如果填写的是：

```text
wss://app.cmtbs.com:57890/ws
```

系统会自动分流，不保证固定走 Whisper。

### 10.3 想用 FunASR，但没有走中文识别

请确认 WebSocket 地址是否填写为：

```text
wss://app.cmtbs.com:57890/ws-gpu1
```

并确认源语言选择为中文。

### 10.4 页面显示原文但没有译文

可能原因：

- 翻译模型还在加载。
- 当前语音片段还没有完成断句。
- 后端翻译服务繁忙。
- 网络连接中断。
- GPU 正在执行其他高显存任务。

可以等待几秒，或停止后重新开始会议。

### 10.5 热词上传后没有明显效果

请检查：

- 热词是否一行一个。
- 是否在开始会议前上传。
- 是否上传了正确文件。
- 热词是否和实际发音差异过大。
- 是否把大段说明文字放进了热词文件。

热词可以提高稳定性，但不能保证每次 100% 命中。

### 10.6 总结生成失败

请检查：

- 是否已经停止会议。
- 是否选择了会议日志。
- 大模型服务是否启动。
- GPU 是否还有足够显存。
- 当前是否还有 ASR 会议正在运行。

建议在没有实时 ASR 任务时再生成总结。

### 10.7 会议中可以换 WebSocket 地址吗

不建议。

如果需要从 Whisper 切换到 FunASR，或从 FunASR 切换到 Whisper，请先停止当前会议，再修改 WebSocket 地址，然后重新开始会议。

## 11. 地址速查

| 用途 | 地址 |
| --- | --- |
| 页面入口 | `https://app.cmtbs.com:57890/` |
| 中控页面 | `https://app.cmtbs.com:57890/admin.html` |
| 自动分流 WebSocket | `wss://app.cmtbs.com:57890/ws` |
| Whisper WebSocket | `wss://app.cmtbs.com:57890/ws-gpu0` |
| FunASR WebSocket | `wss://app.cmtbs.com:57890/ws-gpu1` |
| GPU0 状态检查 | `https://app.cmtbs.com:57890/admin-gpu0/admin/clients` |
| GPU1 状态检查 | `https://app.cmtbs.com:57890/admin-gpu1/admin/clients` |

## 12. 使用建议

- 需要固定使用 Whisper 处理中英文会议：填写 `wss://app.cmtbs.com:57890/ws-gpu0`。
- 需要固定使用 FunASR：填写 `wss://app.cmtbs.com:57890/ws-gpu1`。
- 只是临时使用、不关心后端：填写 `wss://app.cmtbs.com:57890/ws`。
- 会议中不要切换 WebSocket 地址。
- 开始会议前先上传热词。
- 生成总结前必须先停止会议。
- 总结功能显存占用大，尽量在没有 ASR 实时识别任务时使用。
- 如果浏览器缓存导致页面异常，先按 `Ctrl + F5` 强制刷新页面。
